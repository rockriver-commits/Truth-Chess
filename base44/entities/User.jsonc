{
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user"
      ]
    },
    "plan": {
      "type": "string",
      "description": "Subscription tier: 'pro' unlocks full AI levels and unlimited online play.",
      "enum": [
        "free",
        "pro"
      ],
      "default": "free"
    }
  },
  "required": [
    "role"
  ],
  "name": "User",
  "type": "object"
}